  {/*       <NavBar Links={Links} />
      <HeroSection
        Caption={HomePageHeroSectionData.Caption}
        HeroImage={HomePageHeroSectionData.HeroImage}
        ButtonIsVisible
        ButtonCaption={HomePageHeroSectionData.ButtonCaption}
        ButtonUrl={HomePageHeroSectionData.ButtonUrl}
      />
      <CoreValuesSection values={values} />

      <Container>
        <ComponentSlider title={'Our Team Members'} items={members} renderItem={(item) => <TeamMemberCard
          name={item.name}
          email={item.email}
          phoneNumber={item.phoneNumber}
          image={item.image}
          role={item.role}
          description={item.description}
        />} />
      </Container>
      <Container>
        <GridPagination title={'Our Team Members'} items={members} itemsPerPage={3} renderItem={(item) => <TeamMemberCard
          name={item.name}
          email={item.email}
          phoneNumber={item.phoneNumber}
          image={item.image}
          role={item.role}
          description={item.description}
        />}
        seeMoreUrl={'/members'}
        />
      </Container>

      <Container>
        <ComponentSlider title={'Our Partners'} items={partners} itemsPerPage={6} renderItem={(item) => <PartnerCard partner={item} />} />
      </Container>

      <Container>
        <GridPagination title={'Our Partners'} items={partners} itemsPerPage={6} renderItem={(item) => <PartnerCard partner={item} />} />
      </Container>

      <Footer Links={Links} details={details} SociaMediaLinks={SociaMediaLinks} /> */}
